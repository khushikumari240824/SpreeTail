// Balance Controller Placeholders
import db from '../config/db.js';

export const getGroupBalances = async (req, res) => {
  res.status(501).json({ message: 'Get group balances not implemented' });
};

export const getUserBalances = async (req, res) => {
  res.status(501).json({ message: 'Get user balances not implemented' });
};
